import PropTypes from 'prop-types';

const Prepared = () => {
    return (
        <div>
            
        </div>
    );
};
Prepared.propTypes ={
    Prepared:PropTypes.object
}
export default Prepared;